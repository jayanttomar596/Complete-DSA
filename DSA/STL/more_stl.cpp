// MUTLISET

// std::multiset<int> ms;
// ms.insert(1);
// ms.insert(2);
// ms.insert(2);
// ms.insert(3);
// ms now contains {1, 2, 2, 3}







// MULTIMAP

// std::multimap<int, std::string> mmap;
// mmap.insert({1, "apple"});
// mmap.insert({1, "banana"});
// mmap.insert({2, "cherry"});
// mmap now contains {(1, "apple"), (1, "banana"), (2, "cherry")}







// UNORDERED SET

// std::unordered_set<int> uset;
// uset.insert(1);
// uset.insert(2);
// uset.insert(3);
// uset may contain {1, 2, 3}, but the order is not guaranteed







// UNORDERED MAP

// std::unordered_map<int, std::string> umap;
// umap[1] = "apple";
// umap[2] = "banana";
// umap[3] = "cherry";
// umap may contain {1 -> "apple", 2 -> "banana", 3 -> "cherry"}, but order is not guaranteed







// UNORDERED MULTISET

// std::unordered_multiset<int> umset;
// umset.insert(1);
// umset.insert(2);
// umset.insert(2);
// umset.insert(3);
// umset may contain {1, 2, 2, 3}, but the order is not guaranteed







// UNORDERED MULTIMAP

// std::unordered_multimap<int, std::string> ummap;
// ummap.insert({1, "apple"});
// ummap.insert({1, "banana"});
// ummap.insert({2, "cherry"});
// ummap may contain {(1, "apple"), (1, "banana"), (2, "cherry")}, but order is not guaranteed
